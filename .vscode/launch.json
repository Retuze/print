{
    "configurations": [
    {
        "type": "lldb-mi",
        "request": "launch",
        "name": "Launch Program",
        "target": "${fileDirname}\\${fileBasenameNoExtension}.exe",
        "cwd": "${fileDirname}",
        "preLaunchTask": "C/C++: clang.exe 生成活动文件"
    }
    ]
}